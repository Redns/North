@if(!string.IsNullOrEmpty(Content))
{
    <MudStack Class="d-flex mud-width-full justify-center py-2" Style="height:fit-content; text-align:center;">
        @FooterContent
    </MudStack>
}

@code {
    RenderFragment FooterContent => (builder) => builder.AddMarkupContent(0, Content);
}